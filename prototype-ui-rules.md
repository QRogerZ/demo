# Prototype UI Rules

后续所有 UI 修改必须先遵守这个文件。本项目的目标是复刻真实移动端 App 的高保真原型，而不是制作网页、营销页、SaaS dashboard 或通用模板。

## 基本原则

1. 不要重写整个 demo，只改本次反馈相关部分。
2. 不要随意改变整体视觉系统，包括颜色、圆角、阴影、字体、间距和组件层级。
3. 不要新增复杂框架或依赖。
4. 继续使用纯 HTML / CSS / JavaScript。
5. 不使用 React、Next.js、Vite、Tailwind、shadcn 或任何需要安装依赖的框架。
6. demo 必须保持可以直接打开 `index.html` 预览。
7. 所有数据使用 mock data。
8. 不接真实后端、不做真实登录、不做真实支付、不做真实交易。

## UI Kit 使用规则

1. 所有新页面都必须复用已有 UI Kit。
2. 新增组件时，优先扩展 `style.css` 中已有的 CSS variables 和组件结构。
3. 不要为每个模块单独写一套圆角、阴影、边框或字号。
4. 卡片、按钮、输入框、弹窗、底部导航、列表项等基础组件要保持统一。
5. 只有当原版 App 参考资料明确需要时，才新增特殊组件。

## 视觉规则

1. 页面按 390px 宽度的移动端 App 视图设计。
2. 保持真实 App 的视觉密度、字体节奏、卡片层级和交互克制感。
3. 不要大面积渐变背景。
4. 不要过度玻璃拟态。
5. 不要夸张阴影。
6. 不要超大标题。
7. 不要过多营销文案。
8. 不要过度留白。
9. 不要做成通用 AI 模板感很强的页面。
10. 页面第一眼要像真实 App，而不是网页缩进到手机里。

## 交互规则

1. 所有交互都以可演示为目标，不追求真实业务逻辑。
2. 页面切换、弹窗、底部 Sheet、Toast、输入状态等只做前端演示。
3. 不实现真实账号系统、支付流程、交易流程或后端请求。
4. 如果原版 App 有复杂逻辑，先用 mock data 和静态状态表现效果。

## 修改范围规则

1. 每次根据反馈修改时，只处理这次反馈提到的页面、组件或交互。
2. 不因为局部反馈顺手重构全站。
3. 如果必须调整全局视觉系统，需要先用产品语言说明原因。
4. 优先保证与录屏、截图和最新反馈的一致性。

## 判断优先级

如果页面不像真实 App，优先检查并调整：

1. 视觉密度
2. 间距
3. 字体大小和字重
4. 卡片层级
5. 导航结构
6. 列表信息排布
7. 按钮和操作区位置
8. 弹窗和底部 Sheet 的移动端质感
