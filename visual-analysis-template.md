# Visual Analysis Template

后续根据原版 App 录屏 / 截图拆解页面时，按这个模板记录。目标是把视觉和交互先拆清楚，再映射到当前 demo 中逐步复刻。

## 1. 页面名称

填写页面在原版 App 中的名称或临时名称。

## 2. 页面目标

说明这个页面主要帮助用户完成什么动作或理解什么信息。

## 3. 顶部区域结构

记录状态栏、标题栏、返回按钮、右侧操作、搜索框、Tab 或顶部筛选等结构。

## 4. 主内容区域结构

记录页面主体的模块顺序、列表结构、卡片结构、信息分组和滚动方式。

## 5. 底部导航 / 操作区

记录是否有底部导航、常驻按钮、悬浮操作、底部工具栏或底部 Sheet。

## 6. 卡片样式

记录卡片背景、圆角、边框、阴影、内边距、分割线、图片比例和信息层级。

## 7. 字体层级

记录标题、正文、辅助文字、数字、标签、按钮文字的字号、字重和颜色关系。

## 8. 颜色和背景

记录页面背景、主色、文字色、边框色、状态色和特殊高亮色。

## 9. 间距和圆角

记录页面左右边距、模块间距、卡片内边距、列表行高、按钮高度和圆角大小。

## 10. 点击路径

记录从当前页面可以点击到哪些页面或状态，包含点击入口和结果。

## 11. 动效 / 页面切换

记录页面切换、弹窗出现、Sheet 上滑、Toast、加载状态等动效。

## 12. 需要复刻的关键细节

列出最影响真实感和辨识度的细节，优先复刻。

## 13. 可简化的部分

列出可以用 mock data、静态状态或简化交互代替的部分。

## 14. 不要改动的部分

记录本次复刻不应影响的页面、组件或视觉系统。

## 15. 对应到当前 demo 的修改建议

说明需要新增或调整哪些页面、组件、样式和交互。
