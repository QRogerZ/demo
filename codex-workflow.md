# Codex Workflow

这个项目后续会通过自然语言持续迭代。产品经理不需要看代码，也不需要手动纠错。

## 你可以怎样反馈

可以直接用产品语言描述问题，例如：

1. “首页卡片太松了，像网页，不像 App。”
2. “这个弹窗高度不对，应该更像录屏里的底部弹层。”
3. “底部导航的图标和文字比例不对。”
4. “把这个页面按截图 A 重新调整，但不要动其他页面。”
5. “录屏里点击按钮后进入详情页，请补上这个页面切换。”

## 每次修改后 Codex 需要说明

1. 打开哪个文件查看效果。
2. 改了哪些页面。
3. 改了哪些组件。
4. 改了哪些交互。
5. 是否仍然是 mock data。
6. 是否有需要产品确认的地方。

## 产品确认方式

如果需要确认，Codex 应该用产品语言提问，例如：

1. “这个页面更接近列表型首页，还是更接近卡片型首页？”
2. “底部操作区是常驻，还是只在选中内容后出现？”
3. “录屏里没有展示空状态，要不要先按当前 App 风格补一个轻量版本？”

不要问产品经理代码问题，例如：

1. “要不要抽组件？”
2. “这个 class 名怎么命名？”
3. “是否需要改 DOM 结构？”

## 调整优先级

如果页面不像真实 App，优先从以下方向调整：

1. 视觉密度
2. 间距
3. 字体
4. 卡片层级
5. 导航结构
6. 列表和操作区的排布
7. 弹窗、Sheet、Toast 等移动端反馈

## 后续推荐流程

1. 先提供原版 App 录屏。
2. 再提供关键页面截图。
3. 再提供需求文档或功能说明。
4. Codex 根据 `visual-analysis-template.md` 拆解页面。
5. Codex 按页面逐步复刻，不一次性重写整个 demo。
6. 每次修改后，产品经理直接打开 `index.html` 检查效果。
